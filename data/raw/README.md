# Raw Data Placeholder

Place licensed dataset files under this directory before running embedding
generation. Raw videos, frames, audio, and dataset annotations are intentionally
ignored by git.

Expected layout is documented in `../README.md`.
